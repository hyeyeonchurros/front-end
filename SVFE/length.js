const arr=[];
console.log(arr.length);

const arr=[0];
console.log(arr.length);

const arr=[1] = 1;
console.log(arr.length);

const arr=[2] = 2;
console.log(arr.length);

const arr=[100] = 100;
console.log(arr.length);